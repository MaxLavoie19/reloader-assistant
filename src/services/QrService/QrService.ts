import { IQrService } from "@/services/QrService/IQrService";

export class QrService implements IQrService {
  public dataToQr(data: string): void {
    // TODO: convert data to qr code and return the image/path
  }
}
